<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkwisehub.modules.base.mapper.RoleMapper">
    <resultMap id="RoleResultMap" type="com.linkwisehub.modules.base.entity.Role">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="code" column="code"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectAll" resultMap="RoleResultMap">
        SELECT * FROM lwh_base_role
    </select>
    
    <select id="selectById" resultMap="RoleResultMap">
        SELECT * FROM lwh_base_role WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="com.linkwisehub.modules.base.entity.Role">
        INSERT INTO lwh_base_role (name, code, description, status) 
        VALUES (#{name}, #{code}, #{description}, #{status})
    </insert>
    
    <update id="update" parameterType="com.linkwisehub.modules.base.entity.Role">
        UPDATE lwh_base_role 
        SET name = #{name}, code = #{code}, description = #{description}, status = #{status} 
        WHERE id = #{id}
    </update>
    
    <delete id="delete" parameterType="Long">
        DELETE FROM lwh_base_role WHERE id = #{id}
    </delete>
</mapper>
