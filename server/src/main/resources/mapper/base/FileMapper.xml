<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkwisehub.modules.base.mapper.FileMapper">
    <resultMap id="FileResultMap" type="com.linkwisehub.modules.base.entity.File">
        <id property="id" column="id"/>
        <result property="originalName" column="original_name"/>
        <result property="fileName" column="file_name"/>
        <result property="fileSuffix" column="file_suffix"/>
        <result property="fileSize" column="file_size"/>
        <result property="contentType" column="content_type"/>
        <result property="filePath" column="file_path"/>
        <result property="module" column="module"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectAll" resultMap="FileResultMap">
        SELECT * FROM lwh_base_file WHERE status = 1 ORDER BY create_time DESC
    </select>
    
    <select id="selectByModule" resultMap="FileResultMap">
        SELECT * FROM lwh_base_file WHERE module = #{module} AND status = 1 ORDER BY create_time DESC
    </select>
    
    <select id="selectById" resultMap="FileResultMap">
        SELECT * FROM lwh_base_file WHERE id = #{id}
    </select>
    
    <insert id="insert" parameterType="com.linkwisehub.modules.base.entity.File" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO lwh_base_file (original_name, file_name, file_suffix, file_size, content_type, file_path, module, status) 
        VALUES (#{originalName}, #{fileName}, #{fileSuffix}, #{fileSize}, #{contentType}, #{filePath}, #{module}, #{status})
    </insert>
    
    <update id="updateStatus">
        UPDATE lwh_base_file SET status = #{status} WHERE id = #{id}
    </update>
    
    <delete id="delete">
        DELETE FROM lwh_base_file WHERE id = #{id}
    </delete>
</mapper>
