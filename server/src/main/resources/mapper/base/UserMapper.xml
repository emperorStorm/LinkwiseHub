<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.linkwisehub.modules.base.mapper.UserMapper">
    <resultMap id="UserResultMap" type="com.linkwisehub.modules.base.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="organizationId" column="organization_id"/>
        <result property="organizationName" column="organization_name"/>
        <result property="email" column="email"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectAll" resultMap="UserResultMap">
        SELECT u.*, o.name as organization_name 
        FROM lwh_base_user u 
        LEFT JOIN lwh_base_organization o ON u.organization_id = o.id
    </select>
    
    <select id="selectById" resultMap="UserResultMap">
        SELECT u.*, o.name as organization_name 
        FROM lwh_base_user u 
        LEFT JOIN lwh_base_organization o ON u.organization_id = o.id
        WHERE u.id = #{id}
    </select>
    
    <select id="selectByUsername" resultType="com.linkwisehub.modules.base.entity.User">
        SELECT * FROM lwh_base_user WHERE username = #{username}
    </select>
    
    <insert id="insert" parameterType="com.linkwisehub.modules.base.entity.User">
        INSERT INTO lwh_base_user (username, password, real_name, organization_id, email, phone, status) 
        VALUES (#{username}, #{password}, #{realName}, #{organizationId}, #{email}, #{phone}, #{status})
    </insert>
    
    <update id="update" parameterType="com.linkwisehub.modules.base.entity.User">
        UPDATE lwh_base_user 
        <set>
            <if test="username != null">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="realName != null">real_name = #{realName},</if>
            <if test="organizationId != null">organization_id = #{organizationId},</if>
            <if test="email != null">email = #{email},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <delete id="delete" parameterType="Long">
        DELETE FROM lwh_base_user WHERE id = #{id}
    </delete>
    
    <select id="selectByOrganizationId" resultMap="UserResultMap">
        SELECT u.*, o.name as organization_name 
        FROM lwh_base_user u 
        LEFT JOIN lwh_base_organization o ON u.organization_id = o.id
        WHERE u.organization_id = #{organizationId}
    </select>
</mapper>
